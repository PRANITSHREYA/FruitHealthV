
@model FruitHealth.Models.VMLogin
@{
    
   // ViewData["Title"] = "Log in";
}
<div class="container login-register-background">
    <h2>@ViewData["Title"]</h2>

    <!-- Login form content goes here -->
<h1>Login</h1>
    <div class="row">
    <div class="col-md-4">
        <section>
    <form asp-controller="Access" asp-action="Login" method="post">

        <div class="mb-3">
            <input asp-for="Email" class="form-control" aria-required="true" placeholder="email" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <input asp-for="Password" class="form-control" type="password" aria-required="true" placeholder="password" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>
        @* <div class="checkbox mb-3">
            <label asp-for="RememberMe" class="form-label">
                <input class="form-check-input" asp-for="RememberMe" />
                @Html.DisplayNameFor(m => m.Input.RememberMe)
            </label>
        </div> *@
        <label>
            <input type="checkbox" asp-for="KeepLoggedIn" />
            keep me logged In.
        </label><br />
                <button type="submit" class="w-100 btn btn-lg btn-primary">Login</button>
        @if (ViewData["ValidateMessage"] != null)
        {
            <br />
            <br />
            <label>@ViewData["ValidateMessage"]</label>
        }
    </form>
</section>
</div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

</div>